{
  "name": "voistant-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "db:generate": "pnpm --filter @packages/db generate"
  },
  "devDependencies": {
    "prettier": "^3.7.4"
  }
}
